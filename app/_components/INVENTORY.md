# Shared Component Inventory

**Read this before building any component.** If what you need is already here, use it —
do not re-implement. If it's genuinely new, add an entry here as part of the phase that
introduces it (this is a task line in every phase's `tasks.md`, not a courtesy).

Reconcile mobile and desktop Figma component sets into one responsive React component;
handle locale variants via props, not duplicate components.

| Component | File | Used on | Figma node(s) | Notes |
| :--- | :--- | :--- | :--- | :--- |
| Cta | `Cta.tsx` | Every page with a primary action | Mobile `12368:4718`; desktop occurrences `12573:9014`, `12405:6975`, `12358:2031` | Polymorphic `<a>`/`<button>`. `tone: 'dark' \| 'green'` (default `'dark'`) — desktop occurrences split between the two; see `openspec/DECISIONS.md`. Fill-width below 1024px, hug-width above. |
| SecondaryCta | `SecondaryCta.tsx` | Services, any outline CTA | Mobile `12368:4721`; nested in `ServiceCard` `I12220:2696;12220:2660` | Same shape as `Cta`, shares `_buttonStyles.ts`. `tone: 'dark' \| 'light'` (default `'dark'`) for light vs. dark surfaces. |
| Tag | `Tag.tsx` | Portfolio/Artists filters, Project card chips | Outline: `12219:1180`; solid chip: `10274:2297` | Two variants under one name (per proposal scope): `variant="outline"` with `state: 'default' \| 'active'` (white-on-dark, matches Figma's white/opacity-20 tokens); `variant="solid"` with `color: 'neon' \| 'yellow' \| 'orange'`. |
| TitleGroup | `TitleGroup.tsx` | Every section/page header with an eyebrow + heading | Mobile `12219:946`; desktop `12220:1079` (header), `12405:6424` (Home Hero) | `tone: 'dark' \| 'light'` (default `'dark'`), `headingSize: 'h1' \| 'h2'` (default `'h2'`). Home Hero's gradient heading fill is a page-level `className` override, not built in — see D-A in `design.md`. |
| TaglineWrapper | `TaglineWrapper.tsx` | Nested inside `TitleGroup`; usable standalone | `10270:2179` | Always renders rotated 90° — no horizontal occurrence exists in the design. `tone: 'dark' \| 'light'` (default `'dark'`). **Not** used by `ServiceCard` — its numbered tag rotates the opposite direction (`rotate(90deg)` vs. this component's fixed `-90deg`), confirmed a genuine per-instance difference during Phase 3 review; built inline there instead. |
| ProjectCard | `ProjectCard.tsx` | Portfolio grid, Home "Featured Projects" | Mobile `10274:2306`; desktop occurrence `12610:6812` | Presentational shell (D-C) — no MDX/route knowledge. Fixed 353px mobile / 325px desktop width. `image` is optional; renders a neutral placeholder when absent (real project imagery is Phase 11 MDX content). |
| ArtistCard | `ArtistCard.tsx` | Featured Artists | Desktop `12592:6786`; mobile occurrence `12220:1114` | Presentational shell. **Genuinely responsive** (corrected during Phase 3 review — was previously hardcoded to the desktop 320×550 layout at every viewport): `w-full` at mobile (fills its grid cell, `200px`-fixed content block, `193px` image, translucent bio/border colors) vs. fixed `320px` at desktop (`flex-1` content block, `363px` image, solid bio/border colors, bio clamped to 65px). Social links are generic (`iconSrc`/`label`/`href`) — Figma names every icon "Link", no platform baked in. |
| TeamCard | `TeamCard.tsx` | Our Story | Desktop `12610:6405`; mobile occurrence `12217:886` | Presentational shell. Fixed 284px width at both breakpoints. |
| ServiceCard | `ServiceCard.tsx` | Services | Desktop `12600:7460`; mobile occurrence `12220:2696` | **Shell only** — no Services page grid or accordion (D-C). Has its OWN intrinsic padding (corrected during Phase 3 review — not page-section padding a caller adds): mobile `px-[15px] py-[30px]` on a wrapper that's also the card's dark bg (photo bleeds through the wrapper's right padding only); desktop `p-8` on a transparent wrapper. Description overlays the photo at desktop but sits below it on the dark panel at mobile — two `hidden`/`lg:hidden` copies, not one repositioned element. Feature rows are side-by-side at both breakpoints. Numbered tag built inline, not via `TaglineWrapper` (see that row). Composes `SecondaryCta`. |
| Article | `Article.tsx` | News, News Details "Related posts" | Desktop `12612:7696`; mobile occurrence `12220:1670` | Added mid-Phase-2, user-directed (see proposal.md). Presentational shell (D-C) — no MDX/route knowledge; the `CMS` long-form body renderer is still Phase 4. Composes `Tag` (solid variant). Desktop splits side-by-side, mobile stacks with the image last. |
| Nav | `Nav.tsx` | Every page, via `[locale]/layout.tsx` | Desktop `12653:5366` (light) / `12653:5259` (dark); mobile `12219:1100` (carries both as a `Light`/`Dark` property); expanded menu `10270:2118` (EN) / `12368:2386` (TC) | `'use client'` (D-C) — locale switcher + mobile menu state. `theme: 'light' \| 'dark'` (default `'light'`) is a **static per-page prop**, not scroll-driven (D-A) — page phases building a hero pass `theme="dark"` when their Figma frame shows the dark NAV instance (Home does — see `phase-03-shell/design.md` Derived Sources). Collapses to the mobile menu at 1024px (D-E). Desktop's "expanded menu" Figma frame was found to be a stray documentation duplicate with no real trigger — not built. |
| Footer | `Footer.tsx` | Every page, via `[locale]/layout.tsx` | Desktop `12573:9181` (Default) / `12635:16558` (TC); mobile `12384:4852` | Server component. Three groups: Services sub-links (all point at `/services` as a whole page — no anchors exist yet, D-G), page links (from `routes.ts`), and a static newsletter block (no submit handler — real `NewsletterSignup` is Phase 4). Legal links (Privacy/Terms/Cookies) render as inert text, not `<a>`s — no pages exist for them (D-G). |
| Logo | `Logo.tsx` | `Nav`, `Footer` | Desktop `12573:10178` (single `Union` vector, identical path across light/dark NAV variants) | Inline SVG, `fill="currentColor"` — recolor via a `text-*` className on the wrapper (black on light surfaces, white on dark, brand green in the Footer). Sizing: fix a `height` and pair it with `aspect-[97/89]` rather than `w-auto` — an inline `<svg>` with only `width/height` attrs of `100%` doesn't auto-compute width from `w-auto` the way a raster image does. |

Shared button sizing/state classes live in `_buttonStyles.ts` (not itself a component —
imported by `Cta` and `SecondaryCta` so the fill→hug width switch and focus/hover treatment
stay in one place, per D-D/D-F in `openspec/changes/phase-02-primitives/design.md`).
