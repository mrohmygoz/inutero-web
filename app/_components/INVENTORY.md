# Shared Component Inventory

**Read this before building any component.** If what you need is already here, use it —
do not re-implement. If it's genuinely new, add an entry here as part of the phase that
introduces it (this is a task line in every phase's `tasks.md`, not a courtesy).

Reconcile mobile and desktop Figma component sets into one responsive React component;
handle locale variants via props, not duplicate components.

| Component | File | Used on | Figma node(s) | Notes |
| :--- | :--- | :--- | :--- | :--- |
| Cta | `Cta.tsx` | Every page with a primary action | Mobile `12368:4718`; desktop occurrences `12573:9014`, `12405:6975`, `12358:2031` | Polymorphic `<a>`/`<button>`. `tone: 'dark' \| 'green'` (default `'dark'`) — desktop occurrences split between the two; see `openspec/DECISIONS.md`. Fill-width below 1024px, hug-width above. |
| SecondaryCta | `SecondaryCta.tsx` | Services, any outline CTA | Mobile `12368:4721`; nested in `ServiceCard` `I12220:2696;12220:2660` | Same shape as `Cta`, shares `_buttonStyles.ts`. `tone: 'dark' \| 'light'` (default `'dark'`) for light vs. dark surfaces. |
| Tag | `Tag.tsx` | Portfolio/Artists filters, Project card chips | Outline: `12219:1180`; solid chip: `10274:2297` | Two variants under one name (per proposal scope): `variant="outline"` with `state: 'default' \| 'active'` (white-on-dark, matches Figma's white/opacity-20 tokens); `variant="solid"` with `color: 'neon' \| 'yellow' \| 'orange'`. |
| TitleGroup | `TitleGroup.tsx` | ⚠️ **Nothing.** Its only importer is `app/styleguide/_components/PrimitiveSpecimens.tsx` — no page uses it. **Phase 07b deletes it; do not adopt it.** Pages hand-roll the eyebrow + heading instead (nine copies at last count). | Mobile `12219:946` | `tone: 'dark' \| 'light'` (default `'dark'`), `headingSize: 'h1' \| 'h2'` (default `'h2'`), `description` (optional, omits the line entirely when absent). **Genuinely responsive** (corrected during a Phase 3 quick fix — `12220:1079`/`12405:6424`, previously cited as desktop occurrences, turned out to be 393px-wide mobile-frame instances; no shared "Title Group" symbol exists at 1440px, real desktop pages compose the eyebrow + heading inline per-page): below 1024px matches the Figma mobile symbol (fixed `h-[99px] w-[17px]` centered-rotate eyebrow box, `gap-[5px]`, `pr-[15px]`); at 1024px+ keeps the prior derived approximation (`origin-top-left` rotate technique) unchanged — two `lg:hidden`/`hidden lg:*` copies, not one repositioned element, same pattern as `ServiceCard`. Home Hero's gradient heading fill is a page-level `className` override, not built in — see D-A in `design.md`. Eyebrow tag (green dot + rotated label, `10270:2179`) is built inline, not a separate component — it had a single consumer, so the standalone `TaglineWrapper` primitive from Phase 2 was removed as a quick fix after Phase 3. |
| ProjectCard | `ProjectCard.tsx` | Portfolio grid, Home "Featured Projects" | Mobile `10274:2306`; desktop occurrences `12610:6812` (Portfolio), `12210:2404`/`2423`/`2438` (Home) | Presentational shell (D-C) — no MDX/route knowledge. **The card is `w-full`; the caller sets the width** (the earlier "fixed 353px mobile / 325px desktop" note described the Portfolio occurrences, not a constraint the component enforces). Home uses 353px at mobile in both locales, and 400/353/353px at desktop. Rotation and scatter belong to the caller's wrapper. **One order at every width** (corrected in Phase 6 — it shipped image-first): tags -> title -> date -> image -> description. Mobile `10274:2306` and Home's desktop cards `12210:2404` agree. **`12610:6812` is the PORTFOLIO PAGE's card and really is image-first — a different design, not a breakpoint variant.** Reconciling it with a variant prop — never an `lg:` switch — is owed by Phase 10; tracked in `openspec/reference/roadmap.md` → Inherited Work. The image box is `aspect-[333/445]` (the frame's own), not a fixed height, so it scales with the width the caller sets. See D047. `image` is optional; renders a neutral placeholder when absent. Real imagery is owed by Phase 11 (roadmap → Inherited Work). |
| ArtistCard | `ArtistCard.tsx` | Featured Artists | Desktop `12592:6786`; mobile occurrence `12220:1114` | Presentational shell. **Genuinely responsive** (corrected during Phase 3 review — was previously hardcoded to the desktop 320×550 layout at every viewport): `w-full` at mobile (fills its grid cell, `200px`-fixed content block, `193px` image, translucent bio/border colors) vs. fixed `320px` at desktop (`flex-1` content block, `363px` image, solid bio/border colors, bio clamped to 65px). Social links are generic (`iconSrc`/`label`/`href`) — Figma names every icon "Link", no platform baked in. |
| TeamCard | `TeamCard.tsx` | Our Story | Desktop `12610:6405`; mobile occurrence `12217:886` | Presentational shell. Fixed 284px width at both breakpoints. First real consumer: Phase 7's `AboutTeam` composes it in a native scroll-snap row driving 9 real members from `app/_lib/team.ts` (Figma draws only a 5-card Lorem-ipsum sample) — the card's own design is untouched, real bios just clamp/truncate inside its fixed box. |
| ServiceCard | `ServiceCard.tsx` | Services | Desktop `12600:7460`; mobile occurrence `12220:2696` | **Shell only** — no Services page grid or accordion (D-C). Has its OWN intrinsic padding (corrected during Phase 3 review — not page-section padding a caller adds): mobile `px-[15px] py-[30px]` on a wrapper that's also the card's dark bg (photo bleeds through the wrapper's right padding only); desktop `p-8` on a transparent wrapper. Description overlays the photo at desktop but sits below it on the dark panel at mobile — two `hidden`/`lg:hidden` copies, not one repositioned element. Feature rows are side-by-side at both breakpoints. Numbered tag built inline, rotating the opposite direction from `TitleGroup`'s eyebrow tag (see that row). Composes `SecondaryCta`. |
| Article | `Article.tsx` | News, News Details "Related posts" | Desktop `12612:7696`; mobile occurrence `12220:1670` | Added mid-Phase-2, user-directed (see proposal.md). Presentational shell (D-C) — no MDX/route knowledge; the `CMS` long-form body renderer is `Cms.tsx`, added Phase 4. Composes `Tag` (solid variant). Desktop splits side-by-side, mobile stacks with the image last. |
| Nav | `Nav.tsx` | Every page, via `[locale]/layout.tsx` | Desktop `12653:5366` (light) / `12653:5259` (dark); mobile `12219:1100` (carries both as a `Light`/`Dark` property); expanded menu `10270:2118` (EN) / `12368:2386` (TC) | `'use client'` (D-C) — locale switcher + mobile menu state. `theme: 'light' \| 'dark'` (default `'light'`) is a **static per-page prop**, not scroll-driven (D-A) — page phases building a hero pass `theme="dark"` when their Figma frame shows the dark NAV instance (Home does — see `phase-03-shell/design.md` Derived Sources). Collapses to the mobile menu at 1024px (D-E). Desktop's "expanded menu" Figma frame was found to be a stray documentation duplicate with no real trigger — not built. |
| Footer | `Footer.tsx` | Every page, via `[locale]/layout.tsx` | Desktop `12573:9181` (Default) / `12635:16558` (TC); mobile `12384:4852` | Server component. Three groups: Services sub-links (all point at `/services` as a whole page — no anchors exist yet, D-G), page links (from `routes.ts`), and a static newsletter block (**a different design from `NewsletterSignup`** — confirmed by the Phase 4 survey, so it was not refactored to compose it; its button is `type="button"` since no endpoint exists, D029). Legal links (Privacy/Terms/Cookies) render as inert text, not `<a>`s — no pages exist for them (D-G). |
| UniversalCTA | `UniversalCTA.tsx` | Home, Services, and any page ending in the green CTA block | Desktop `12653:5649` (EN `12573:9014` / CN `12653:5650`); mobile symbol `12212:5282`, instanced `12220:2432` (Services) and `12368:2429` (TC Home) | Full-bleed green gradient section. **Genuinely responsive** — mobile is a `justify-between` column (heading / button / contact), desktop puts the heading top-left and pairs button + contact in a row near the bottom; two copies, not one repositioned tree. Backgrounds differ too: `universal-cta-glow.svg` in `mix-blend-darken` at mobile vs. the full-bleed `universal-cta-grain.png` at desktop, with gradients at different angles. Composes `Cta` with `tone="dark"`. Desktop heading is a locally-drawn 288px (no token carries it, D012) split across two nowrap lines; mobile uses `Display/Jumbo` and wraps to four. **Headline and "General Inquiries" stay English in both locales** — that is what the CN/TC frames show (D030). |
| NewsletterSignup | `NewsletterSignup.tsx` | News, News Details, any page with the dark subscribe band | Desktop `12612:8163`; mobile symbol `12212:6048`, instanced `12212:6049` and `12389:6080` (TC) | Server component. Optional `action` — **without it the field and button are inert markup** (`type="button"`, no `<form>`), because no newsletter endpoint exists and a form with no action reloads the page (D029). Composes `Cta` with `tone="green"`. Mobile stacks the form and adds `border-y`; desktop is a right-aligned `max-w-750` row with no borders. **Not the same as the Footer's newsletter block** — the Phase 4 survey confirmed they are different designs; `Footer.tsx` keeps its own. |
| Cms | `Cms.tsx` | News Details, Portfolio Details — any MDX body | Desktop `12610:7361`; mobile occurrence `12211:4514`, TC `12368:2711` | Article-body renderer for compiled MDX. Takes `body` (the MDX default export from `app/_lib/content`) and passes its element map as a `components` prop — **not** via `MDXProvider`, which is React context and would force `'use client'`. The root `mdx-components.tsx` is intentionally empty so this treatment never applies globally (D025). Implements the five designed elements only; **the share groups in the Figma node are not part of `Cms`** (D027) — the bottom row shipped as `ShareRow` (D031), the desktop-only left rail is unbuilt and owed by Phase 11/14 (roadmap → Inherited Work). Lists, links, `h1`/`h3`, `hr` are derived, not design-matching. Two load-bearing fixes: a paragraph whose only child is the figure unwraps (markdown puts a lone image in `<p>`, which would nest `<figure>` invalidly), and the blockquote unwraps its nested paragraph (otherwise a 45px display quote renders at 16px). |
| ShareRow | `ShareRow.tsx` | News Details, Portfolio Details — below any MDX body | Desktop `12612:8414`/`12612:8416`; mobile occurrence `12211:4535` / `12220:1887`; X glyph from the left rail `12610:7350` | `'use client'` — a real share intent needs the page's absolute URL and no site origin is configured (no `metadataBase`), so the URL is read via `useSyncExternalStore` rather than an effect. Composed by `ContentDetail`, **not** by `Cms` — it acts on the page, not the body. Four targets: copy-link, LinkedIn, X, Facebook. **The third glyph is the left rail's X, not the bottom row's YouTube** — YouTube has no share endpoint, so that icon could only be dead (D031). The desktop-only left share rail is still not built — owed by Phase 11/14 (roadmap → Inherited Work). |
| Logo | `Logo.tsx` | `Nav`, `Footer` | Desktop `12573:10178` (single `Union` vector, identical path across light/dark NAV variants) | Inline SVG, `fill="currentColor"` — recolor via a `text-*` className on the wrapper (black on light surfaces, white on dark, brand green in the Footer). Sizing: fix a `height` and pair it with `aspect-[97/89]` rather than `w-auto` — an inline `<svg>` with only `width/height` attrs of `100%` doesn't auto-compute width from `w-auto` the way a raster image does. **Desktop only** — mobile uses `LogoMark` (see next row). |
| LogoMark | `LogoMark.tsx` | `Nav` (mobile bar + expanded menu) | Mobile `12563:4308` (48×48, in NAV `12212:6237`); menu mark `10270:2157` (115×115) | Inline SVG, `fill="currentColor"`, square `viewBox="0 0 48 48"` — size with `size-*`, no aspect helper needed. This is the seal mark **without** the "IN UTERO" wordmark that `Logo` carries; the two lockups are a deliberate breakpoint difference in the design, not drift. Brand green in the light mobile bar (paired with the 225px rule that runs off the left edge through the mark), white on dark and inside the green menu overlay. |

Shared button sizing/state classes live in `_buttonStyles.ts` (not itself a component —
imported by `Cta` and `SecondaryCta` so the fill→hug width switch and focus/hover treatment
stay in one place, per D-D/D-F in `openspec/changes/phase-02-primitives/design.md`).
